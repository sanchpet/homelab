---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base/3x-ui
  - httproute.yaml
  # anylink (OpenConnect SSL-VPN) — app-template workload (base) + cluster-specific
  # config/cert/secret here:
  - ../base/anylink
  - anylink-config.yaml
  - anylink-certificate.yaml
  - anylink-secret.sops.yaml
