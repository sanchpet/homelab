---
# The chart isn't published to a Helm repo, so consume it from git, pinned to the exact
# commit reviewed (combined ovpn-admin+openvpn image confirmed at this SHA). v0.0.3.
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: ovpn-admin
  namespace: flux-system
spec:
  interval: 1h
  url: https://github.com/flant/ovpn-admin
  ref:
    commit: ee35520b1a4f44726f4d1bb7cde5954a7784a21a
