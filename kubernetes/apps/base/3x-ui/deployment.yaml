---
# 3x-ui — Xray panel (VLESS/Reality etc). Userspace proxy: no TUN/privileged needed
# (unlike anylink). hostNetwork so configured inbounds bind the node's public ports
# directly. Single node → hostPath persistence for the config DB and certs.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: 3x-ui
  namespace: vpn
  labels:
    app: 3x-ui
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: 3x-ui
  template:
    metadata:
      labels:
        app: 3x-ui
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - name: 3x-ui
          image: ghcr.io/mhsanaei/3x-ui:latest   # TODO: pin a version once chosen
          env:
            - name: XUI_ENABLE_FAIL2BAN
              value: "true"
          volumeMounts:
            - name: db
              mountPath: /etc/x-ui
            - name: cert
              mountPath: /root/cert
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
      volumes:
        - name: db
          hostPath:
            path: /var/lib/3x-ui/db
            type: DirectoryOrCreate
        - name: cert
          hostPath:
            path: /var/lib/3x-ui/cert
            type: DirectoryOrCreate
