---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gost-tls
  namespace: vpn
spec:
  secretName: gost-tls
  dnsNames:
    - gost.${cluster_subdomain}
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
