---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: anylink-tls
  namespace: vpn
spec:
  secretName: anylink-tls
  dnsNames:
    - anylink.${cluster_subdomain}
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
