---
# Sandbox capability selection for this cluster (learning workloads only). Selects the
# whole sandbox catalog (shared source + all learning apps); no per-cluster customs.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../../apps/base/sandbox
