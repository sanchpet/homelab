---
# Sandbox capability selection for this cluster (learning workloads only). Points at
# the pure base; no per-cluster customs yet, so it's a plain passthrough.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../../apps/base/sandbox/incident-agent
