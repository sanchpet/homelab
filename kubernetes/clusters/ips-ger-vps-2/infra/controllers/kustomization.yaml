---
# Capability selection — controllers layer. The Vault host runs the shared platform
# (cert-manager + NGF + reloader) for its TLS gateway.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../../infra/bundles/platform
