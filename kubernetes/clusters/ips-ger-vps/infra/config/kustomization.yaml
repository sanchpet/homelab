---
# Capability selection — config layer. public-tls (issuers) + vpn-gateway (shared gateway
# + sub cert, parameterized). public-tls runtime-needs platform (taken in controllers).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../../infra/bundles/public-tls
  - ../../../../infra/bundles/vpn-gateway
