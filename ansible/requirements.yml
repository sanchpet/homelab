# Community-зависимости — пинятся здесь, не вендорятся в репо.
# Установка:  ansible-galaxy install -r requirements.yml
collections:
  - name: kubernetes.core
    version: ">=6.4.0"
  - name: ansible.posix
    version: ">=2.2.0"
  # База для роли hardening (CIS/NSA, battle-tested). Major запинен — мажор может ломать.
  - name: devsec.hardening
    version: ">=10.6.0,<11.0.0"
  # Официальная k3s-роль (k3s team, @dereknola, maintained). Git-источник, пин на тег.
  - name: https://github.com/k3s-io/k3s-ansible.git
    type: git
    version: "1.2.1"

roles: []
