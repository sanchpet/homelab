# Topology only — variables live in group_vars/ and host_vars/.
# ansible_host is a DNS name (public, not a secret); the IP can change behind it.
# Groups follow the k3s.orchestration convention (k3s_cluster → server/agent).
# Dedicated single-node cluster for HashiCorp Vault (secret store, WP-042).
[server]
ips-ger-vps-2 ansible_host=vps-2.ger.ips.sanch.pet

[agent]

[k3s_cluster:children]
server
agent
