# Line endings: normalize all text to LF in the repo and on checkout, on every
# platform. Pairs with the pre-commit `mixed-line-ending` hook.
* text=auto eol=lf

# GitHub Linguist
# YAML is a "data" language, which Linguist excludes from the repo language bar by
# default — force it to be counted (this repo is YAML-heavy: Kubernetes + Flux).
*.yaml linguist-detectable=true
*.yml  linguist-detectable=true

# ...but Flux bootstrap output (GitOps Toolkit, gotk-*) is generated, not authored —
# exclude it from the stats and collapse it in diffs. gotk-components.yaml alone is
# ~89% of this repo's YAML lines, so it would otherwise drown out the real config.
**/flux-system/gotk-*.yaml linguist-generated=true
